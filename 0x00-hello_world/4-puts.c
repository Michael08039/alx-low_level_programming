#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    return (0);
}
        put("\"programming is like building a multilingual puzzle");
	return (0);
}
